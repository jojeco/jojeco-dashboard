import { Server } from 'lucide-react';
import { PlaceholderPage } from './PlaceholderPage';

export default function ServicesPage() {
  return (
    <PlaceholderPage
      title="Services"
      icon={Server}
      message="Service detail view coming in slice 2"
      action="Health counts visible on Home"
    />
  );
}
